import 'package:flutter/material.dart';

const TextStyle headingStyle = TextStyle(
  fontSize: 20,
  fontWeight: FontWeight.bold,
  color: Colors.black,
);

const TextStyle timeStyle = TextStyle(fontSize: 16, color: Colors.black87);

const Color primaryColor = Color(0xFF0D9488); // Teal (Main)
const Color bgColor = Color(0xFFF0FDF4); // Light greenish background
const Color cardColor = Color(0xFFFFFFFF); // White
const Color textPrimary = Color(0xFF1F2937); // Dark gray
const Color textSecondary = Color(0xFF6B7280); // Light gray
const Color accentColor = Color(0xFF10B981); // Emerald
